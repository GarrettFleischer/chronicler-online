// import { Meteor } from 'meteor/meteor';

// if (Meteor.isClient) {
//   subscribed = false;
//   Tracker.autorun(() => {
//     if (Meteor.user() && !subscribed) Meteor.subscribe('users');
//     subscribed = true;
//   });
// }
